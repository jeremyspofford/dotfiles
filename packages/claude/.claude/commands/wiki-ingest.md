Read `$WIKI_VAULT/CLAUDE.md` first, then execute the `/wiki ingest` workflow defined there.

Usage: /wiki ingest [domain/project]

No argument = ingest all pending raw notes with ingested: false across all domains.
