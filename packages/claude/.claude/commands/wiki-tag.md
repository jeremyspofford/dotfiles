Read `$WIKI_VAULT/CLAUDE.md` first, then execute the `/wiki tag propose` workflow defined there.

Usage: /wiki tag propose <tagname> [--category <category>]

Never use an unapproved tag. Always propose and get explicit approval before adding to the registry.
